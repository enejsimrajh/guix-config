Currently only NVMe drives are supported.

Sanitize, encrypt, and swap steps are optional but recommended.

Reboot and set the root password if all the steps have completed successfully:
$ reboot
$ passwd root